import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: {
    template: '%s | Startup MB',
    default: 'Startup MB — Myrtle Beach Startup Ecosystem',
  },
  description: 'Mapping the Myrtle Beach startup ecosystem. Discover local companies, founders, and innovators building the future in the Grand Strand.',
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" className="h-full">
      <body className={`${inter.className} min-h-full`}>{children}</body>
    </html>
  );
}
